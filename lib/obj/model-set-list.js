var ModelSet = require("./model-set");function getFileds (filedInfo) {    var fields = {};    for(var key in filedInfo){        if(filedInfo[key]["auto_increment"]){            continue;        }        fields[key] = filedInfo[key];    }    return fields;}function ModelSetList(obj,filedInfo){    this.setList = [];    var fields = getFileds(filedInfo);    for(var key in fields){        if(typeof obj[key] == "undefined"){            continue;        }        var value = obj[key];        if(filedInfo[key].type.toUpperCase() == "JSON" && typeof value == "object"){          value = JSON.stringify(value);        }        this.setList.push(new ModelSet(key,value).toString());    }}ModelSetList.prototype.toString = function(){    return this.setList.join(",");}module.exports = ModelSetList;